user:
  type: object
  properties:
    id:
      type: integer
    email:
      type: string
      format: email
    profile_type:
      type: string
    profile_id:
      type: integer
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
