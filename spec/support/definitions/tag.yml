tag:
  type: object
  properties:
    id:
      type: integer
    title:
      type: string
    consumer_id:
      type: integer
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time