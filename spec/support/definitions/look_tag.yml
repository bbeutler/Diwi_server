look_tag:
  type: object
  properties:
    id:
      type: integer
    look_id:
      type: integer
    tag_id:
      type: integer
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time